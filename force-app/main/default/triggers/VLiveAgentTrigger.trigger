trigger VLiveAgentTrigger on LiveChatTranscript(before insert, after insert, before update, after update, before delete, after delete, after undelete) {

    //TriggerFactory.createAndExecuteHandler(VLiveAgentTriggerHandler.class);
       
}